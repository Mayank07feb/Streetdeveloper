<!-- ================= Header Section ================= -->
<header
  class="header-wrapper w-full fixed top-0 left-0 border-b border-gray-200 py-0 z-20 transition-all duration-300 ease-in-out bg-white">

  <!-- Menu overlay for mobile -->
  <div class="menu-overlay fixed inset-0 bg-black/40 hidden z-40 lg:hidden"></div>

  <div class="max-w-screen-xl mx-auto px-3 sm:px-6 md:px-14 lg:px-14 xl:px-18 2xl:px-3 pb-0">
    <nav class="flex items-center justify-between w-full relative">

      <!-- Logo -->
      <a class="flex items-center lg:py-4 py-3 mr-5 font-montserrat font-bold text-xl text-gray-900" href="/">
        <img src="/assets/img/logo.png" alt="Street-Developer Logo" class="lg:h-14 h-12 mr-2" loading="eager">
        STREET DEVELOPER
      </a>


      <!-- Menu -->
      <div
        class="menu-block hidden lg:block fixed lg:static top-0 left-0 w-full lg:w-auto h-full lg:h-auto bg-white lg:bg-transparent z-50 p-6 lg:p-0 overflow-y-auto">

        <!-- Mobile menu header inside menu block -->
        <div class="flex items-center justify-between lg:hidden mb-6">
          <a href="/" class="logo-sub flex items-center font-montserrat">
            <img src="/assets/img/logo.png" alt="Street-Developer Logo" loading="lazy" class="h-8 mr-2">
            STREET DEVELOPER
          </a>

          <!-- Close button for mobile menu -->
          <div class="menu-close cursor-pointer">
            <svg viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="1.5" fill="none"
              stroke-linecap="round" stroke-linejoin="round" class="css-i6dzq1">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
          </div>
        </div>

        <!-- Menu links -->
        <ul
          class="flex flex-col lg:flex-row lg:gap-6 gap-1 font-montserrat lg:font-semibold font-medium text-[17px] text-gray-900">
          <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2" href="/">Home</a>
          </li>
          <!-- <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2" href="/services">Services</a>
          </li> -->
          <!-- <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2" href="/projects">Projects</a>
          </li> -->
          <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2" href="/about">About</a>
          </li>
          <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2"
              href="/products">Products</a>
          </li>
          <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2" href="/review">Review</a>
          </li>
          <li>
            <a class="nav-link flex items-center lg:justify-normal justify-between gap-1 lg:py-10 py-2"
              href="/contact">Contact</a>
          </li>
        </ul>

        <!-- Mobile "Enquiry Now" button -->
        <div class="mt-auto flex flex-col lg:hidden">
          <a href="#"
            class="flex items-center justify-center gap-2 px-5 py-3 text-white bg-primary rounded-lg text-base font-medium hover:bg-primary-hover transition-all duration-200 aos-init aos-animate">
            <span>Enquiry Now</span>
          </a>

        </div>

      </div>

      <!-- Right icon / desktop "Enquiry Now" -->
      <div class="flex items-center space-x-4">
        <a href="#"
          class="md:flex hidden items-center justify-center gap-2 px-5 py-3 text-white bg-primary rounded-lg text-base font-medium hover:bg-primary-hover transition-all duration-200 aos-init aos-animate">
          <span>Enquiry Now</span>
        </a>

        <!-- Mobile menu trigger -->
        <button aria-label="menu" class="mobile-menu-trigger px-0 text-gray-700 flex lg:hidden">
          <svg viewBox="0 0 24 24" width="24" height="24" stroke="currentColor" stroke-width="2" fill="none"
            stroke-linecap="round" stroke-linejoin="round" class="text-gray-900">
            <line x1="3" y1="12" x2="21" y2="12"></line>
            <line x1="3" y1="6" x2="21" y2="6"></line>
            <line x1="3" y1="18" x2="21" y2="18"></line>
          </svg>
        </button>
      </div>

    </nav>
  </div>
</header>
<!-- ================= End Header Section ================= -->



<!-- Modern Enquiry Modal with Phone Number -->
<div id="enquiryModal" class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center hidden z-50 p-4">
  <div
    class="bg-white rounded-2xl w-full max-w-lg md:max-w-md p-6 md:p-8 relative shadow-2xl transform transition-all scale-95 opacity-0 animate-modalShow max-h-[90vh] overflow-y-auto">
    <!-- Close Button -->
    <button id="closeModal"
      class="absolute top-4 right-4 text-gray-400 hover:text-gray-800 text-3xl font-bold transition">&times;</button>

    <!-- Header -->
    <h2 class="text-2xl font-bold text-gray-900 mb-4 text-center">Get in Touch</h2>
    <p class="text-gray-500 mb-6 text-center text-sm md:text-base">Fill out the form below and we'll get back to
      you shortly.</p>

    <!-- Form -->
    <form class="space-y-4">
      <div>
        <label class="block text-gray-700 mb-1 font-medium text-sm">Name</label>
        <input type="text"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 md:px-4 md:py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition text-sm md:text-base"
          placeholder="Your Name">
      </div>
      <div>
        <label class="block text-gray-700 mb-1 font-medium text-sm">Email</label>
        <input type="email
          "
          class="w-full border border-gray-300 rounded-lg px-3 py-2 md:px-4 md:py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition text-sm md:text-base"
          placeholder="Your Email">
      </div>
      <div>
        <label class="block text-gray-700 mb-1 font-medium text-sm">Phone Number</label>
        <input type="tel"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 md:px-4 md:py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition text-sm md:text-base"
          placeholder="Your Phone Number">
      </div>
      <div>
        <label class="block text-gray-700 mb-1 font-medium text-sm">Message</label>
        <textarea rows="4"
          class="w-full border border-gray-300 rounded-lg px-3 py-2 md:px-4 md:py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition text-sm md:text-base"
          placeholder="Your Message"></textarea>
      </div>
      <button type="submit"
        class="w-full bg-primary text-white font-medium px-5 py-3 rounded-lg hover:bg-primary-hover transition shadow-md hover:shadow-lg text-base flex items-center justify-center gap-2">
        Send Message
      </button>

    </form>
  </div>
</div>

<!-- Tailwind Animation -->
<style>
  @keyframes modalShow {
    0% {
      opacity: 0;
      transform: scale(0.95);
    }

    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-modalShow {
    animation: modalShow 0.3s ease-out forwards;
  }

  .nav-link {
    transition: all 0.2s ease;
    padding-left: 0.75rem;
    padding-right: 0.75rem;
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;
  }

  .nav-link.active {
    background-color: #186fbc;
    color: white;
    border-radius: 0.5rem;
  }

  @media (max-width: 1023px) {
    .nav-link {
      padding-left: 0;
      padding-right: 0;
      padding-top: 0.5rem;
      padding-bottom: 0.5rem;
    }
    
    .nav-link.active {
      padding-left: 1rem;
      padding-right: 1rem;
    }
  }
</style>
<script>
  // Select all "Enquiry Now" buttons (mobile + desktop)
  const enquiryButtons = document.querySelectorAll('a[href="#"].bg-blue-600, a[href="#"].bg-primary');
  const modal = document.getElementById('enquiryModal');
  const closeModal = document.getElementById('closeModal');

  // Open modal on click
  enquiryButtons.forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      modal.classList.remove('hidden');
    });
  });

  // Close modal on close button click
  closeModal.addEventListener('click', () => {
    modal.classList.add('hidden');
  });

  // Close modal if clicked outside content
  window.addEventListener('click', (e) => {
    if (e.target === modal) {
      modal.classList.add('hidden');
    }
  });

  // ===== Mobile Menu Toggle =====
  const mobileMenuTrigger = document.querySelector('.mobile-menu-trigger');
  const menuBlock = document.querySelector('.menu-block');
  const menuClose = document.querySelector('.menu-close');
  const menuOverlay = document.querySelector('.menu-overlay');

  function openMenu() {
    menuBlock.classList.remove('hidden');
    menuOverlay.classList.remove('hidden');
    document.body.classList.add('overflow-hidden');
  }
  function closeMenu() {
    menuBlock.classList.add('hidden');
    menuOverlay.classList.add('hidden');
    document.body.classList.remove('overflow-hidden');
  }

  if (mobileMenuTrigger) mobileMenuTrigger.addEventListener('click', openMenu);
  if (menuClose) menuClose.addEventListener('click', closeMenu);
  if (menuOverlay) menuOverlay.addEventListener('click', closeMenu);
  window.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeMenu(); });

  // ===== Active Nav Link =====
  const currentPath = window.location.pathname;
  const navLinks = document.querySelectorAll('.nav-link');
  
  navLinks.forEach(link => {
    const linkPath = new URL(link.href).pathname;
    if (linkPath === currentPath) {
      link.classList.add('active');
    }
  });
</script>